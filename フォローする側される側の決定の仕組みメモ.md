# フォローする側される側が決定する仕組み
RelationshipsController の create メソッドがフォローする側とされる側を決定し、Relationship モデルにデータを書き込む仕組みは以下の通り。

コントローラーの create メソッド
```ruby:/app/controllers/relationships_controller.rb
def create
  @user = User.find(params[:followed_id])  # フォローされる側のユーザーを取得
  current_user.follow(@user)  # 現在のユーザーが @user をフォローする
end
```
このメソッドの処理は以下のように進む：

ユーザーの取得: User.find(params[:followed_id]) でフォローされるユーザー（@user）をデータベースから取得。このとき params[:followed_id] にはフォームから送信されたフォローされるユーザーの ID が含まれている。
```html:/app/views/users/_follow.html.erb
<%= form_with(model: current_user.following_relationships.build, local: false) do |f| %>
  <div>
    <%= hidden_field_tag :followed_id, @user.id %>
  </div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
```
フォームの仕組み
form_with(model: current_user.following_relationships.build, local: false) は、Railsのフォームヘルパーで、current_user.following_relationships.build からフォームを作成します。model: オプションに渡すオブジェクトは、フォームの送信先やデータを含むモデルを指定
<%= hidden_field_tag :followed_id, @user.id %> は、followed_id という名前の隠しフィールドを作成。隠しフィールドは、ユーザーが直接見ることなくフォームデータとしてサーバーに送信される。
@user.id は、followed_id フィールドに設定される値。この値が、params ハッシュに含まれることで、コントローラーアクションでアクセスできる。
params に含まれる仕組み
フォームの送信:ユーザーが「Follow」ボタンをクリックすると、フォームが送信される。送信されるデータには、フォーム内のすべてのフィールド（この場合は followed_id）が含まれる。
ーバーサイドでの処理:フォームデータは、HTTPリクエストとしてサーバーに送信される。Railsは、このリクエストデータを params ハッシュとしてコントローラーアクションに渡す。
例えば、RelationshipsController の create アクションが呼ばれると、params ハッシュに followed_id が含まれており、これを使用して適切なユーザーを特定できる。




フォローアクションの呼び出し: current_user.follow(@user) で、現在のユーザー（current_user）が @user をフォローするアクションが実行されます。この follow メソッドは User モデル内で定義されています。

User モデルの follow メソッド
```ruby:/app/models/user.rb
def follow(other_user)
  following_relationships.find_or_create_by(followed_id: other_user.id)
end
```

このメソッドの流れは以下のようになります：

following_relationships: current_user の following_relationships は、follower_id が current_user.id である全ての Relationship レコードを示します。

find_or_create_by: このメソッドは、指定された followed_id を持つレコードを following_relationships から検索し、見つからなければ新しく作成します。ここで follower_id は自動的に current_user.id になります。

Relationship モデルの belongs_to 関連
Relationship モデル内の belongs_to 関連は、followed_id がどのユーザーを指しているのかを示します：

```ruby::/app/models/relationship.rb
class Relationship < ApplicationRecord
  belongs_to :follower, class_name: 'User'
  belongs_to :followed, class_name: 'User'
end
```


source: :followed の役割
source: :followed は、following_users 関連で followed_id を取得するために使用されます

```ruby:/app/models/user.rb
has_many :following_users, through: :following_relationships, source: :followed
```
source: :followed: following_relationships 関連を通じて followed にアクセスし、followed_id カラムの値を取得します。これにより、following_users がフォローしているユーザーのリストとして取得できるようになります。
まとめ
フォローする側とされる側: RelationshipsController の create メソッドで、current_user（フォローする側）と @user（フォローされる側）が決まります。
source: :followed: following_users の関連において、followed_id カラムに対応するユーザーを取得するために使用されます。Relationship モデルで belongs_to :followed と定義しているため、正しいユーザーが関連付けられます。
この仕組みによって、フォローのアクションが正しくデータベースに保存され、関連するユーザーが取得できるようになります。

## 自分なりまとめ
source: :followedと書くだけで、followed_idを参照できるようになるのはRelationshipモデルに対応するマイグレーションファイルで以下のように記述しているから。t.references :followedにより、followed_idカラムが自動で生成され、結果的にRelationshipモデルはidカラム２列のテーブルになっている。
```
t.references :follower, null: false, foreign_key: { to_table: :users }
t.references :followed, null: false, foreign_key: { to_table: :users }
```
userモデルの
has_many :following_users, through: :following_relationships, source: :followedは、フォローしているユーザーの集まりをfollowing_relationships上のfollowed_idを使って、following_usersというテーブル（ユーザーの集まり）を作ったもの。following_relationships自体はfollower_idをキーにして、Relationshipモデルとおりfollowed_idを集めたもの。